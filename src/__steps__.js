/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export it as default app
 * 5. Build> Authentication> Get started> enable sign in method
 * 6. Create sign up and login route
 */

/**
 * 1. Create a context provider file
 * 2. Create a context and set provider
 * 3. Set the children props
 * 4. Set context value
 * 5. Set the provider
 */

/**
 * Hosting
 *
 * One time per PC:
 * 1. npm install -g firebase-tools
 * 2. firebase login
 *
 * For each project one time
 * 1. Firebase init
 * 2. reply 'Y/n' to proceed
 * 3. Hosting: firebase (use keyboard up and down arrow to move) and (use space bar to select)
 * 4. Select existing project
 * 5. and choose the project name from firebase (Carefully!!!)
 * 6. Which project as public directory: dist (vite) / build (create react app)
 * 7. Single page application? (answer: yes)
 * 8. Continue deploy with github (answer: no)
 *
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 */
